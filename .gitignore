node_modules/
frontend/node_modules/
__pycache__/
*.py[cod]
.env
claude_desktop_config.json
.env.neon-backup-*
aws-postgres.env
# SSH keys and local infra state (never commit real hosts/passwords)
*.pem
apps/role-propagator/role-propagator
data/
/companies/
/kuchup.com-audit/
.idea/
.coverage
.pytest_cache/
/.claude/
/.testtt/
/CLAUDE.md
**/.DS_Store
/dist/
/dist/mcp-resume-reframe-for-claude.zip
# Operator-local EC2 payloads (Caddyfile, Alloy, tectonic-warm) live on the
# laptop and the server today. Track public templates under deploy/ec2/ when
# they are reviewed into git — see docs/operations/ec2-deploy.md.
/deploy/*
!/deploy/ec2/
/deploy/ec2/*
!/deploy/ec2/README.md
relocation_jobs/static/dist/.source-hash
relocation_jobs/static/homepage/.source-hash
/design-skills/
# Vendor / agent skill installs (use skills-lock locally; not product code)
.agents/
.cursor/skills/
skills-lock.json
# Hallmark session artifacts (design.md is the kept source of truth)
.hallmark/
# Local responsive/review captures (underscore-prefixed)
**/brand/_*.png
/.cursor/
# Local internal-docs documents (contents ignored; folder stays in the repo)
/internal-docs/
